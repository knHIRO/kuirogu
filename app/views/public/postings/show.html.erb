投稿詳細画面
<div class="container">
    <h2>商品詳細画面</h2>
	<div class="row">

    </div>
    <div class="col-md-5">

      <%= form_with model: [:public, @posting] do |f| %>
      <%= link_to new_public_posting_path(@posting.id), class: "col-md-4" do %>
      <%= image_tag @posting.get_profile_image(250,250) %>
      <% end %>

      <div class="form-group row form-inline ">
      <%= f.label :body, "本文", class: "col-md-4" %>
      <span>
        <%= @posting.body %>
      </span>

      </div>

      <% end %>
    </div>
  </div>
</div>